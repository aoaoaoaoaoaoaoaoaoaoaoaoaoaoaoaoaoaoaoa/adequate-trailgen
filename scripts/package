#!/bin/sh
set -eu

root=$(CDPATH= cd -- "$(dirname -- "$0")/.." && pwd)
cd "$root"

for package in trailgen-contract trailgen-core trailgen-data trailgen-gui trailgen; do
    cargo package --locked --package "$package"
done
